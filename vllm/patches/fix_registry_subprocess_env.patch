diff --git a/vllm/model_executor/models/registry.py b/vllm/model_executor/models/registry.py
index 362028ebf..7b26de8a5 100644
--- a/vllm/model_executor/models/registry.py
+++ b/vllm/model_executor/models/registry.py
@@ -1,5 +1,6 @@
+import traceback
@@ -766,6 +766,58 @@ def _try_inspect_model_cls(
         return model.inspect_model_cls()
     except Exception:
         err_text = traceback.format_exc()
         is_sycl_no_device = "No device of requested type available" in err_text
         log_traceback = os.environ.get("VLLM_LOG_TRACEBACK", "0") == "1"

         if is_sycl_no_device:
             logger.error(
                 "Model inspection failed: SYCL runtime could not find a usable "
                 "device. Set VLLM_TARGET_DEVICE=cpu (or "
                 "VLLM_SKIP_MODEL_INSPECT=1) to continue."
             )
         else:
             logger.error(
                 "Error in inspecting model architecture '%s'",
                 model_arch,
                 exc_info=log_traceback,
             )

         if is_sycl_no_device and log_traceback:
             logger.debug("Full model inspection traceback", exc_info=True)
+        if os.environ.get("VLLM_SKIP_MODEL_INSPECT", "0") == "1":
+            logger.warning(
+                "VLLM_SKIP_MODEL_INSPECT is set; returning default "
+                "ModelInfo for '%s'", model_arch)
+            return _ModelInfo(
+                architecture=model_arch,
+                is_text_generation_model=True,
+                is_pooling_model=False,
+                attn_type="decoder",
+                default_seq_pooling_type="LAST",
+                default_tok_pooling_type="ALL",
+                supports_cross_encoding=False,
+                supports_multimodal=True,
+                supports_multimodal_raw_input_only=False,
+                requires_raw_input_tokens=False,
+                supports_multimodal_encoder_tp_data=False,
+                supports_pp=True,
+                has_inner_state=False,
+                is_attention_free=False,
+                is_hybrid=False,
+                has_noops=False,
+                supports_mamba_prefix_caching=False,
+                supports_transcription=False,
+                supports_transcription_only=False,
+            )
         return None
 
 
@@ -1183,8 +1208,19 @@ def _run_in_subprocess(fn: Callable[[], _T]) -> _T:
 
         # cannot use `sys.executable __file__` here because the script
         # contains relative imports
+        # On XPU, the subprocess must not initialize SYCL/Level Zero
+        # devices. Force CPU target and strip Intel GPU env vars to
+        # avoid the SYCL "No device of requested type available" crash
+        # during model inspection.
+        sub_env = os.environ.copy()
+        sub_env["VLLM_TARGET_DEVICE"] = "cpu"
+        for key in list(sub_env.keys()):
+            if key.startswith(("ZE_", "SYCL_")):
+                del sub_env[key]
+        sub_env.pop("ONEAPI_DEVICE_SELECTOR", None)
         returned = subprocess.run(
-            _SUBPROCESS_COMMAND, input=input_bytes, capture_output=True
+            _SUBPROCESS_COMMAND, input=input_bytes, capture_output=True,
+            env=sub_env,
         )
 
         # check if the subprocess is successful
