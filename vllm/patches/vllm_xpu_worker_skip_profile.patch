--- vllm/v1/worker/xpu_worker.py	2026-04-14 19:39:54.213875700 +0900
+++ vllm/v1/worker/xpu_worker.py	2026-04-14 21:35:16.872088911 +0900
@@ -124,24 +124,33 @@
         # cache blocks that can be allocated with the remaining free memory.
         torch.xpu.empty_cache()
         torch.xpu.synchronize()
-        torch.xpu.reset_peak_memory_stats()
 
-        # Execute a forward pass with dummy inputs to profile the memory usage
-        # of the model.
-        if "Qwen3ASR" in self.model_runner.model_config.architecture:
-            pass
-        else:
-            self.model_runner.profile_run()
+        skip_profile = os.getenv("VLLM_SKIP_PROFILE_RUN", "0") == "1"
+        arch = self.model_runner.model_config.architecture
+        # Skip profile_run for architectures known to hang or OOM during profiling
+        skip_archs = ("Qwen3ASR", "Glm4MoeLite", "Gemma4")
 
-        # Calculate the number of blocks that can be allocated with the
-        # profiled peak memory.
-        torch.xpu.synchronize()
-        total_gpu_memory = torch.xpu.get_device_properties(self.local_rank).total_memory
+        if not skip_profile and not any(a in arch for a in skip_archs):
+            torch.xpu.reset_peak_memory_stats()
+            self.model_runner.profile_run()
 
-        # NOTE(woosuk): Here we assume that the other processes using the same
-        # GPU did not change their memory usage during the profiling.
-        stats = torch.xpu.memory_stats()
-        peak_allocated = stats.get("allocated_bytes.all.peak", 0)
+            # Calculate the number of blocks that can be allocated with the
+            # profiled peak memory.
+            torch.xpu.synchronize()
+            total_gpu_memory = torch.xpu.get_device_properties(
+                self.local_rank).total_memory
+            stats = torch.xpu.memory_stats()
+            peak_allocated = stats.get("allocated_bytes.all.peak", 0)
+        else:
+            total_gpu_memory = torch.xpu.get_device_properties(
+                self.local_rank).total_memory
+            # Skip profile_run — estimate peak from model memory + 5% overhead
+            # MoE models only activate a fraction of experts per token,
+            # so runtime overhead above model weights is minimal
+            used_memory = torch.xpu.memory_allocated()
+            peak_allocated = int(used_memory * 1.05)
+            print(f"\n[VLLM_SKIP_PROFILE_RUN] Skipping profile_run for {arch}, "
+                  f"estimating peak from allocated memory * 1.05")
         current_reserved = torch.xpu.memory_reserved()
 
         fragmentation_bytes = current_reserved - peak_allocated
